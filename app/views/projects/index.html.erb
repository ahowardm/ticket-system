<h1><%= t('activerecord.models.project', count: @projects.size) %></h1>

<ul>
    <% @projects.each do |p| %>
        <li><%= p.name %> |
          <%= link_to 'Eliminar', project_path(p), data: { turbo_method: :delete, turbo_confirm: "Estás seguro?" } %> |
          <%= link_to 'Editar', edit_project_path(p) %></li>
    <% end %>
</ul>

<%= link_to t('views.project.new'), new_project_path %>
