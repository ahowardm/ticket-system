<%= simple_form_for @ticket do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.select :project_id, @projects %>
  <%= f.select :ticket_status_id, @ticket_statuses %>
  <%= f.button :submit %>
<% end %>
